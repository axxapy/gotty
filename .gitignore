/out
/js/node_modules
/js/dist
/.idea
/*.iml
# bun build artifact; regenerated by `make assets`. Not shipped in git
# (the bundle itself is, since it's go:embed-ed into the binary).
/assets/static/js/gotty-bundle.js.map
